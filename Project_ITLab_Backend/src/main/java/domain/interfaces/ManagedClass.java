package domain.interfaces;

/**
 * Represents a class that should be managed by an InstanceManager and cached.
 * Only one constructor should exist for these classes containing dependencies.
 */
public interface ManagedClass {
}
